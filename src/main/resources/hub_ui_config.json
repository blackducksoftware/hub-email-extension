{
  "extensionMetaData": {
    "id": "hub-email-extension"
  },
  "configurationElements": [
    {
      "name": "optIn",
      "type": "boolean",
      "title": "Opt In \/ Opt Out",
      "required": true,
      "description": "whether or not emails should be triggered from published notifications"
    },
    {
      "name": "emailFrequency",
      "type": "string",
      "title": "Email Frequency",
      "required": false,
      "description": "how often the Hub should be checked for notifications that might trigger emails",
      "allowedValues": [
        "daily",
        "weekly",
        "monthly"
      ]
    },
    {
      "name": "templateName",
      "type": "string",
      "title": "Template Name",
      "required": false,
      "description": "the template for email rendering"
    },
    {
      "name": "emailTriggers",
      "type": "array",
      "title": "Reasons To Trigger Emails",
      "required": false,
      "description": "the user can specify any number of reasons that will trigger an email",
      "allowedValues": [
        "Policy Violation",
        "Policy Violation Override Cancellation",
        "Override Policy Violation",
        "Security Vulnerabilities"
      ]
    },
    {
      "name": "emailTriggeringProjects",
      "type": "array",
      "title": "Projects To Trigger Emails",
      "required": false,
      "description": "the user can specify any number of projects that will trigger an email"
    },
    {
      "name": "emailTriggeringPolicies",
      "type": "array",
      "title": "Policies To Trigger Emails",
      "required": false,
      "description": "the user can specify any number of policies that will trigger an email"
    }
  ]
}
